/**
 * Accelerator normalization. Re-exported from `@excalibur/shared` (ESM build),
 * replacing the former duplicated copy. Behaviour is covered by the shared tests.
 */
export { normalizeAccelerator } from '@excalibur/shared';
export type { KeyEventLike } from '@excalibur/shared';
