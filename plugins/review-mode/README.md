# Review Mode (first-party)

Local-only review annotations — no cloud, no realtime.

- Leave comment pins on a drawing, reply, and resolve.
- Stored alongside the workspace in `.excalibur/reviews/`, so a folder shared
  between sequential reviewers carries the comments with it.

Contributes the *Open Review panel* command. `filesystem: workspace-only`,
`network: none`.
